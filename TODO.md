# TODO

- [x] Update `public/css/style.css` để `.inner-avatar img` mặc định `animation-play-state: paused` và chỉ quay khi JS bật lên.
- [x] Update `public/js/script.js` để bắt đúng state pause/play của APlayer (đảm bảo dừng ngay khi bấm pause, và quay lại khi phát tiếp).
- [x] Nâng cấp nhẹ UI trong `public/css/style.css` (base styles, typography, hover/shadow đồng bộ).
- [x] Chạy build/dev để kiểm tra trang songs/detail: pause => dừng, play tiếp => quay.
